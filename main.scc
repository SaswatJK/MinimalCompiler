{
    x : int = 2 + 3;
    y : int = x * 2;
    if(x < 100){
        print( (y + 2) * x);
    } else {
        if(x < 75) {
            print(y);
        } else {
            print(y + x);
        };
    };
}
